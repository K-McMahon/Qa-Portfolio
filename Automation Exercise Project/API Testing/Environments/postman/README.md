# Postman Workspace Files

These YAML files mirror the collection and environment in Postman's version-control format. They make individual requests reviewable alongside the exported JSON collection.
