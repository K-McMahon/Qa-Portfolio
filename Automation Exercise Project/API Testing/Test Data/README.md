# API Test Data

This folder is reserved for portfolio-safe CSV or JSON data used by Postman collection runs.

- Do not store passwords, access tokens, production data, or personal information here.
- Keep secret values in local Postman environment variables.
- Use unique disposable email addresses for account-lifecycle runs.
- Record the data file name in the API test repository when a data-driven run is executed.
