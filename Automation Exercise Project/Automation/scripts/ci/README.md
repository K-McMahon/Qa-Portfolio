# CI Reporting Helpers

These deterministic Node.js helpers create sanitized run summaries, post allowlisted Jira comments, and generate monthly portfolio summaries. Unit tests cover request safety, aggregation, links, and output formatting.
