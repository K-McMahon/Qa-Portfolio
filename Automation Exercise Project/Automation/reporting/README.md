# QA Reporting

The custom reporter produces a stakeholder-facing analytics report and copies sanitized evidence into traceable Test Case filenames. Supporting modules build checkout and order evidence bundles.
