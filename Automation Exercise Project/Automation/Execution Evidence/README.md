# Playwright Execution Evidence

This folder contains sanitized screenshots, short videos, invoice text, and generated evidence bundles from mapped Playwright scenarios. Filenames use the related Test Case ID so each item can be traced to the RTM and Jira.
